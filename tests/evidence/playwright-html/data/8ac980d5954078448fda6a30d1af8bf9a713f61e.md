# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: regressivo-canary.spec.js >> Regressivo Canary >> CT-REG-004 — Criação de contato via UI
- Location: tests\e2e\regressivo-canary.spec.js:234:3

# Error details

```
Error: POST /contacts deve retornar 201

expect(received).toBe(expected) // Object.is equality

Expected: 201
Received: 0
```

# Test source

```ts
  243 | 
  244 |     page.on('response', async resp => {
  245 |       if (
  246 |         resp.url().match(/\/v3\/accounts\/[^/]+\/contacts$/) &&
  247 |         resp.request().method() === 'POST'
  248 |       ) {
  249 |         postStatus   = resp.status();
  250 |         const body   = await resp.json().catch(() => null);
  251 |         responseUuid = body?.uuid ?? body?.data?.uuid ?? null;
  252 |       }
  253 |     });
  254 | 
  255 |     await page.goto('/chat', { waitUntil: 'domcontentloaded' });
  256 | 
  257 |     // Navegar para a seção de contatos (tenta múltiplos seletores de nav)
  258 |     const contactsNavSelectors = [
  259 |       'a[href*="/contacts"]',
  260 |       'a[href*="/contatos"]',
  261 |       '[data-testid="contacts-nav"]',
  262 |       '[data-testid="nav-contacts"]',
  263 |       'nav >> text=/^contatos$/i',
  264 |       'aside >> text=/^contatos$/i',
  265 |       '[aria-label*="contatos" i]',
  266 |     ];
  267 | 
  268 |     let navigatedToContacts = false;
  269 |     for (const sel of contactsNavSelectors) {
  270 |       const el = page.locator(sel).first();
  271 |       if (await el.isVisible({ timeout: 1_500 }).catch(() => false)) {
  272 |         await el.click();
  273 |         navigatedToContacts = true;
  274 |         break;
  275 |       }
  276 |     }
  277 | 
  278 |     if (!navigatedToContacts) {
  279 |       // Fallback: tenta URL direta
  280 |       await page.goto('/contacts', { waitUntil: 'domcontentloaded' }).catch(() => {});
  281 |       navigatedToContacts = !(await page.url().includes('/login'));
  282 |     }
  283 | 
  284 |     if (!navigatedToContacts) {
  285 |       console.warn('  ⚠️ Não foi possível navegar para a seção de contatos — verifique a URL da rota de contatos');
  286 |       test.skip();
  287 |     }
  288 | 
  289 |     // Botão de novo contato (texto varia por versão do SPA)
  290 |     const newContactBtn = page.locator([
  291 |       'button:has-text("Adicionar Novo")',
  292 |       'button:has-text("Novo contato")',
  293 |       'button:has-text("Novo Contato")',
  294 |       '[data-testid="new-contact-btn"]',
  295 |       '[data-testid="create-contact"]',
  296 |       'button:has-text("+ Contato")',
  297 |       'button:has-text("Adicionar")',
  298 |     ].join(', ')).first();
  299 | 
  300 |     await expect(newContactBtn).toBeVisible({ timeout: 10_000 });
  301 |     await newContactBtn.click();
  302 | 
  303 |     // Preenche o formulário
  304 |     const nameInput = page.locator([
  305 |       'input[name="name"]',
  306 |       'input[placeholder*="nome" i]',
  307 |       'input[placeholder*="Name" i]',
  308 |     ].join(', ')).first();
  309 |     await expect(nameInput).toBeVisible({ timeout: 8_000 });
  310 |     await nameInput.fill(contactName);
  311 | 
  312 |     // Telefone — componente react-phone-input-2
  313 |     // Usa DDD 21 (Rio) em formato E.164: evita ambiguidade DDD 55 vs CC 55
  314 |     // NÃO limpa antes de fill() — limpar reseta o país selecionado
  315 |     const phoneInput = page.locator('input[type="tel"]').first();
  316 |     await expect(phoneInput).toBeVisible({ timeout: 5_000 });
  317 |     await phoneInput.fill(contactPhone);        // E.164: +5521987xxxxxx
  318 |     // Dispara blur para que o componente valide o número
  319 |     await nameInput.click();
  320 |     await page.waitForTimeout(500);
  321 | 
  322 |     // Verifica se o botão ficou habilitado após preencher os campos
  323 |     const saveBtn = page.locator([
  324 |       'button[type="submit"]',
  325 |       'button:has-text("Salvar")',
  326 |       'button:has-text("Criar")',
  327 |       'button:has-text("Cadastrar")',
  328 |     ].join(', ')).first();
  329 | 
  330 |     // Aguarda o botão habilitar (validação async do form)
  331 |     await expect(saveBtn).toBeEnabled({ timeout: 5_000 }).catch(async () => {
  332 |       // Se ainda desabilitado, loga o estado atual para diagnóstico
  333 |       const phoneVal = await phoneInput.inputValue().catch(() => 'N/A');
  334 |       const nameVal  = await nameInput.inputValue().catch(() => 'N/A');
  335 |       console.log(`  [FORM] nome="${nameVal}" | phone="${phoneVal}" | saveBtn ainda disabled`);
  336 |     });
  337 | 
  338 |     await saveBtn.click({ force: true }); // force=true para clicar mesmo se estiver desabilitado
  339 | 
  340 |     // Aguarda resposta da API
  341 |     await page.waitForTimeout(3_000);
  342 | 
> 343 |     expect(postStatus, 'POST /contacts deve retornar 201').toBe(201);
      |                                                            ^ Error: POST /contacts deve retornar 201
  344 |     expect(responseUuid, 'UUID do contato criado deve existir').toBeTruthy();
  345 | 
  346 |     createdContactUuid = responseUuid;
  347 |     console.log(`  Contato criado: ${createdContactUuid}`);
  348 |   });
  349 | 
  350 |   // ── CT-REG-005 — Edição inline de contato (ContactData.tsx) ─────────────────
  351 |   test('CT-REG-005 — Edição de contato via página de contatos', async ({ page }) => {
  352 |     const suffix    = String(Date.now()).slice(-6);
  353 |     const testEmail = `argos.qa.reg${suffix}@test.poli.digital`;
  354 | 
  355 |     let patchStatus = 0;
  356 | 
  357 |     page.on('response', async resp => {
  358 |       if (
  359 |         resp.url().match(/\/v3\/accounts\/[^/]+\/contacts\/[^/?]+$/) &&
  360 |         resp.request().method() === 'PATCH'
  361 |       ) {
  362 |         patchStatus = resp.status();
  363 |       }
  364 |     });
  365 | 
  366 |     // Resolve qual URL de contato usar:
  367 |     // 1) CT-REG-004 criou um contato → navega direto para ele
  368 |     // 2) Fallback → abre a lista de contatos e pega o primeiro
  369 |     let contactHref = null;
  370 | 
  371 |     if (createdContactUuid) {
  372 |       contactHref = `/contacts/${createdContactUuid}`;
  373 |       console.log(`  Usando contato do CT-REG-004: ${createdContactUuid}`);
  374 |     } else {
  375 |       await page.goto('/contacts', { waitUntil: 'domcontentloaded' });
  376 | 
  377 |       const addBtn = page.locator('button:has-text("Adicionar Novo"), button:has-text("Adicionar")');
  378 |       await expect(addBtn.first()).toBeVisible({ timeout: 20_000 });
  379 | 
  380 |       const firstContactLink = page.locator('a[href*="/contacts/"]').first();
  381 |       const hasLink = await firstContactLink.isVisible({ timeout: 5_000 }).catch(() => false);
  382 | 
  383 |       if (!hasLink) {
  384 |         console.log('  ⚠️ Nenhum link de contato encontrado — CT-REG-005 BLOQUEADO');
  385 |         test.skip();
  386 |         return;
  387 |       }
  388 | 
  389 |       contactHref = await firstContactLink.getAttribute('href');
  390 |       if (!contactHref) {
  391 |         console.log('  ⚠️ href do contato não encontrado — CT-REG-005 BLOQUEADO');
  392 |         test.skip();
  393 |         return;
  394 |       }
  395 | 
  396 |       console.log(`  Usando primeiro contato da lista: ${contactHref}`);
  397 |     }
  398 | 
  399 |     // Navega para a página de detalhe / edição do contato
  400 |     await page.goto(contactHref, { waitUntil: 'domcontentloaded' });
  401 |     await page.waitForTimeout(3_000);
  402 | 
  403 |     // Ativa modo de edição se necessário
  404 |     const emailInput = page.locator('input[type="email"], input[name="email"]').first();
  405 |     if (!await emailInput.isVisible({ timeout: 4_000 }).catch(() => false)) {
  406 |       const editBtn = page.locator([
  407 |         'button:has-text("Editar")',
  408 |         'text="Clique para editar"',
  409 |         '[data-testid="edit-contact"]',
  410 |         '[aria-label*="editar" i]',
  411 |       ].join(', ')).first();
  412 | 
  413 |       if (await editBtn.isVisible({ timeout: 3_000 }).catch(() => false)) {
  414 |         await editBtn.click();
  415 |         await page.waitForTimeout(500);
  416 |       }
  417 |     }
  418 | 
  419 |     await expect(emailInput).toBeVisible({ timeout: 8_000 });
  420 |     await emailInput.selectText().catch(() => {});
  421 |     await emailInput.fill(testEmail);
  422 | 
  423 |     const saveBtn = page.locator([
  424 |       'button[type="submit"]',
  425 |       'button:has-text("Salvar")',
  426 |       'button:has-text("Salvar alterações")',
  427 |     ].join(', ')).first();
  428 | 
  429 |     await expect(saveBtn).toBeVisible({ timeout: 5_000 });
  430 |     await saveBtn.click();
  431 | 
  432 |     await page.waitForTimeout(2_000);
  433 | 
  434 |     expect(patchStatus, 'PATCH /contacts/:uuid deve retornar 200').toBe(200);
  435 |     console.log(`  PATCH /contacts → ${patchStatus} | email: ${testEmail}`);
  436 |   });
  437 | 
  438 |   // ── Teardown ────────────────────────────────────────────────────────────────
  439 |   test.afterAll(async ({ browser }) => {
  440 |     if (!createdContactUuid) return;
  441 | 
  442 |     // Reutiliza a sessão salva para fazer a chamada de delete
  443 |     const ctx  = await browser.newContext({ storageState: 'tests/fixtures/canary-manager-storageState.json' });
```